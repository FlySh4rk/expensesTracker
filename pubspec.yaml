name: expenses_tracker
description: Offline-first personal budgeting MVP.
version: 0.1.0+1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  drift: ^2.19.0
  drift_flutter: ^0.2.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.4
  path: ^1.9.0
  uuid: ^4.4.0
  intl: ^0.19.0
  csv: ^6.0.0
  share_plus: ^10.0.2
  shared_preferences: ^2.3.2

  collection: ^1.18.0

flutter:
  uses-material-design: true


dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.19.0
  build_runner: ^2.4.12
  flutter_lints: ^4.0.0
